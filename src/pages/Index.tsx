// Update this page (the content is just a fallback if you fail to update the page)

import VerotejiditsLanding from "../components/VerotejiditsLanding";

const Index = () => {
  return <VerotejiditsLanding />;
};

export default Index;
